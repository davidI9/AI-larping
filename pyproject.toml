[project]
name = "ai-larping"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "david", email = "davidsanchezpalomino200603@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
ai-larping = "ai_larping:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
